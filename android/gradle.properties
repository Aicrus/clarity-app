org.gradle.jvmargs=-Xmx4096m -XX:MaxMetaspaceSize=512m -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8 -Dkotlin.daemon.jvm.options="-Xmx2048m"
android.useAndroidX=true
android.enableJetifier=true
android.enableR8=true

# Enable Gradle build cache for faster builds
org.gradle.caching=true

# Enable parallel builds
org.gradle.parallel=true

# Enable configuration cache (experimental - can speed up builds but may cause issues)
# Disabled due to compatibility issues with current Gradle/Android setup
# org.gradle.configuration-cache=true

# Enable daemon for faster subsequent builds
org.gradle.daemon=true

# Enable incremental compilation
org.gradle.configureondemand=true
